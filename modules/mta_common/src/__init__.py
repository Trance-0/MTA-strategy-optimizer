"""Implementation package for the shared canonical data model.

Holds every provider-independent dataclass and enum that
``mta_attribution``, ``mta_standard``, ``mta_strategy_recommendation``, and
the dashboard schema can converge on, plus the compatibility layer that
adapts today's Amazon-specific structures to and from it. See
``docs/en/introduction/data-models/`` for the specification these classes
implement.
"""
