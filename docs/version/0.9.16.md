---
title: Version 0.9.16
description: Recorded the UI_design reference prototype's move from external/ to design-artifacts/
compact: "Recorded a plain git-detected rename of external/UI_design/ (the AI-MTA demo HTML and the brandlens-vue prototype) to design-artifacts/UI_design/, matching the documented workspace-partition split between external/ for pinned third-party repositories and design-artifacts/ for historical product vision. No file content changed."
order: 1
---

# Version 0.9.16

Date: 2026-08-18

## Changes

- Recorded the move of `external/UI_design/` to `design-artifacts/UI_design/`, already performed on disk outside Git. `git add` detected it as a pure rename for every file: the AI-MTA demo HTML page and the full `brandlens-vue` reference prototype (27 files). No file content changed. This aligns the directory with the workspace partitions already documented in `docs/en/introduction/index.md`: `external/` for the pinned MTA-SIM-dataset repository, `design-artifacts/` for historical product vision.

## Verification

- `git status` shows every path under the old and new locations as a detected rename (`R`), not a delete-plus-add.
