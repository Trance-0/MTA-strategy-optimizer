---
title: Version 0.9.51
description: Reconcile the remote analysis workbench with local dashboard and contributor-history changes
compact: "Integrates origin/main workbench release 0.9.48 with local option rows, deployment identity and shared logs; retains the unpublished contributor-history description after its release-number collision."
order: -29
---

# Version 0.9.51

Date: 2026-09-09

## Integration

- Merge the remote dataset-driven analysis workbench with local option-row layouts, attribution boundary fixes, deployment identity, and shared operational logs.
- Preserve registered-dataset status alongside deployment-mode styling and Settings refresh identity.
- Keep the published remote 0.9.48 page intact. The previously uncommitted local page with the same number is retained in full below; 0.9.49 and 0.9.50 retain their existing descriptions. Archive 0.9.47 so the newest four patches remain flat.

## Integration Verification

- All 176 dashboard, 190 backend, and 100 strategy-evaluation tests pass.
- Dashboard production and English documentation builds pass; existing large-chunk notices remain. The dashboard fixture import encountered a transient Windows rename error on its first attempt and passed on retry.
- The merge retains both parent histories and the pre-sync stash as a recovery copy. No remote push or deployment was performed.

## Preserved Local Contributor-History Changes


- Replaced the project manager's one-line role description with a `## Scope`
  section derived from the 79 commits authored under that identity between
  2026-08-03 and 2026-09-07. It names five areas — the pipeline and canonical
  data model, attribution and strategy, dashboard and backend, deployment and
  mirrors, and the documentation set with its repository rules — and links the
  areas owned by others to their own pages. `ZheyuanWu.md` was the only
  contributor page without such a section.
- Corrected that page's `compact` and `description` fields, which credited the
  Data Generator configuration workflow and preflight to the project manager.
  That work belongs to Chenghao Jin and is recorded on his page and in
  [0.9.46](0.9/0.9.46.md); the fields now describe the areas Git attributes here.
- Added a `Git author` line to all six work-log pages, recording each person's
  `Name <email>` exactly as `git log` reports it, including Jiahao Yao's second
  early identity and the absence of any commit for Tianle Chen, whose database
  work lives in external service schemas. Gave Chenghao Jin's page the
  `Name (handle)` title and the active-period line the other pages carry.
- Recorded both as work-log conventions in `docs/worklog/index.md`, and added
  each person's Git author identity to the roster beside their responsibility.

## Earlier Local Verification

- The English documentation build passes. The six work-log pages and the
  roster render, and every cross-page link added by the new scope section
  resolves to a built page. The only build notice was the existing chunk-size
  warning.
- Each recorded identity was read from `git log` over all references rather
  than inferred from a display name. The scope areas were checked against the
  per-directory author counts for the paths they claim.
- Every work-log page remains far below the 500-line limit, and abbreviations
  introduced by the new section are expanded on first use.

## Earlier Local Limits

Tianle Chen has no commit in this repository, so their page records the absence
rather than an identity. Yayu Yu commits under a personal name with no separate
account handle, so that page carries no `Handle` line. The 0.9.46 page and the
`_bmad-output/` artifacts still describe the Data Generator release itself and
were not rewritten; only the misattributed routing fields were corrected.
