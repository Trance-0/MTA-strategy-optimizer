---
title: Versions 0.9.1–0.9.2
description: Archived summary of base-aware Pages and Gitee configuration followed by the exact English architecture-diagram translation
compact: "Historical record only: archived versions 0.9.1 and 0.9.2, covering base-aware Pages redirects, HTTPS Gitee target support, version ownership, and the exact English translation of the original three-page system architecture diagram. Read for change provenance."
order: 80
---

# Versions 0.9.1–0.9.2

Released: 2026-08-12

This page consolidates two adjacent historical versions so the version log retains at most ten pages. It preserves the material changes and verification evidence from both original pages.

## Version 0.9.1

### Changes

- Made the documentation-root redirect and site-title link aware of the deployed site root, so GitHub Pages sends readers to `/MTA-strategy-optimizer/en/` while local previews continue to use `/en/`.
- Allowed `GITEE_REPOSITORY` to use either an HTTPS `gitee.com` repository URL or the shorter `owner/repository.git` form, without allowing embedded credentials.
- Defined version ownership: project managers control major versions, human developers control minor versions, and agents increment only patch versions unless explicitly directed otherwise.

### Verification

- Build the VitePress site with `DOCS_BASE_PATH=/MTA-strategy-optimizer/` and inspect the generated root redirect.
- Validate the GitHub Actions workflow syntax and exercise repository-target normalization with the configured HTTPS URL shape.

## Version 0.9.2

### Changes

- Replaced the independently designed root architecture diagram with an English translation derived directly from `docs/系统架构图-07.drawio`.
- Preserved all three source pages and every original cell, component, style, relationship, geometry, and position.
- Regenerated the root SVG embedded by the project README from the translated Draw.io source.
- Consolidated historical versions 0.1 and 0.2 so the version-log directory remains within its ten-page limit.

### Verification

- Compared the source and translated Draw.io XML structurally, excluding only page names and cell-label values.
- Confirmed that all three pages have matching cell counts and that no Chinese characters remain in the translated source.
- Rendered and visually inspected the translated application-architecture page.
