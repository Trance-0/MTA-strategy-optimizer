---
title: Version 0.9.46
description: Completed lossless configuration and authoritative preflight for the Data Generator
compact: "Completed the Data Generator's lossless Guided/JSON editor, configuration cards, authoritative side-effect-free preflight, lifecycle race guards, static capability boundary, backend reuse, bounded error mapping, and regression coverage while retaining preview, download, and PostgreSQL export contracts."
order: -24
---

# Version 0.9.46

Date: 2026-09-07

## Changes

- Replaced the partial Data Generator form with a Guided/JavaScript Object
  Notation (JSON) editor for the complete reviewed configuration. Guided and
  JSON editing retain unknown fields, provenance, null, zero, and false values;
  the editor validates visible fields, preserves ordered touchpoint and path
  references, and requires confirmation before discarding an edit for another
  preset or variant.
- Added an authoritative, side-effect-free preflight endpoint. It returns
  bounded field and section issues, invokes the pinned Multi-Touch Attribution
  Simulator (MTA-SIM) loader for final validation, and is reused before a run
  allocates an identifier, directory, retained state, or background operation.
- Bound accepted preflight and run-poll responses to the exact current
  configuration and opaque run token. Late responses cannot enable a changed
  configuration or replace current feedback. Static deployments explain the
  full-stack requirement and refuse generator operations without creating
  backend requests; existing preview, download, and backend-only PostgreSQL
  export contracts remain intact.

## Verification

- The dashboard suite reported 141 passing tests, including 31 Data Generator
  workflow cases. The complete backend suite reported 151 passing tests, with
  all 31 focused Data Generator tests passing against the pinned simulator.
- Production and static Vue builds passed, as did the English documentation
  build, the scoped specification ownership gate, and Ruff checks for the
  changed backend files. The only build notice was the existing Plotly
  chunk-size warning.
- Browser acceptance confirmed both reviewed variants, keyboard-accessible
  controls, 610px and 1280px layouts without page-level horizontal overflow,
  preview/download/export continuation, and the static unavailable state.

## Limits

GitHub Pages remains a read-only demonstration without a generator backend.
Generation, download, and PostgreSQL export require a local or container
full-stack deployment with the pinned MTA-SIM checkout available.
