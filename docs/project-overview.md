# 工作区总览与现状评价

本仓库不是单一源码目录，而是“业务模块 + 本地 Agent/BMad 工具链 + 项目知识与
历史记录”的复合工作区。AMC MTA 是可运行归因实现；MTA Strategy Initializer
已经提供 Campaign Group 顶层数据契约、运行条件模拟数据和校验，但尚未实现完整策略
生成流水线。

## 结论

当前工作区整体处于**结构清楚、可复现、适合继续研发，但业务模型仍是验证型
Demo**的状态。

- AMC MTA 的用户事件事实源、四类派生数据、五份输出、文档和107项测试一致，流水线可确定性复现。
- 项目当前事实、历史愿景、外部研究和实现记录已经分区，不再把未实现功能描述为
  当前能力。
- `.agents` 与 `_bmad` 是安装型开发工具，不是营销产品源码；119 个已声明技能与
  119 个直接技能目录完全对应。
- 扫描未发现空文件、符号链接、缓存字节码、新增临时文件或未归位业务文件；工作区
  保留4个既有的macOS `.DS_Store`元数据文件，它们不是业务产物。
- 41份非规格项目Markdown全部可从根入口到达且没有本地断链；105个Python文件和7个JavaScript
  文件均通过语法检查。
- 工具层存在一个已确认的上游测试断言不一致；另有一个工具测试依赖 `pytest`，
  当前环境未安装，因此没有执行。两项都不影响 AMC MTA 运行。

## 工作区规模

工作区文件数与字节数会随工具包和实现记录变化，精确值以机器清单为准。清单排除
`.git` 内部对象、清单自身、ignored 的个人配置以及受保护文件：

| 分区 | 文件数 | 大小 | 角色 |
| --- | ---: | ---: | --- |
| `.agents` | 以机器清单为准 | 以机器清单为准 | 安装后的 Agent/工作流技能 |
| `_bmad` | 以机器清单为准 | 以机器清单为准 | BMad 安装配置、清单、WDS 共享运行资料；不含 ignored 个人覆盖 |
| `_bmad-output` | 以机器清单为准 | 以机器清单为准 | 已完成规格、当前整理记录与延期事项 |
| `design-artifacts` | 以机器清单为准 | 以机器清单为准 | 早期产品愿景、模型说明与决策记录 |
| `docs` | 以机器清单为准 | 以机器清单为准 | 当前文档、研究原件和扫描状态；不含清单自身 |
| `modules` | 以机器清单为准 | 以机器清单为准 | AMC MTA 与策略初始化模块 |
| 根目录文件 | 以机器清单为准 | 以机器清单为准 | 导航、忽略规则与格式配置；不含受保护日志 |

逐文件路径、大小、权限和 SHA-256 见
[`workspace-file-inventory.json`](workspace-file-inventory.json)。

## 分区评价

### 业务实现：`modules/amc_mta`

定位为 AMC 匿名聚合路径上的五段多触点归因诊断 Demo。当前优势是契约清楚、
输出完整、Markov 与 Path-level Shapley 双模型治理逻辑明确、测试覆盖较强。
主要限制是使用模拟数据、没有滚动窗口与重采样稳定性证据、没有真实 AMC
隐私执行验证，也不具备因果增量或自动预算能力。

详细评价见 [AMC MTA 能力评价](amc_mta/amc-mta-capability-assessment.md)。

### 策略初始化：`modules/mta_strategy_recommender`

现阶段实现 `Campaign Group → Campaign → 新 Ad Group 数量与预算` 的确定性生成和 AMC
跨模块只读校验。两个输入 JSON 给出候选计数、产品容量、最低预算和 AMC 血缘；当前容量
真实计算为 `1/1/1/1`。全部 17 个 MTA 触点经 AMC `assisted_*` bridge 汇总成 Campaign
份额，再在同 Campaign 的匿名新组内等分。输出不含具体候选、Targeting 或动作；结果交给
下游优化，不声称最高 ROI、因果增量或全局最优。

### 项目知识：`docs`

当前事实、模块架构、能力边界、研究原件和历史愿景已分层；位置规则见
[工作区文件位置管理](workspace-file-management.md)。7 份 PDF、2 份 DOCX
和 Amazon Ads OpenAPI JSON 是研究输入，不是运行依赖。

### 历史追溯：`design-artifacts` 与 `_bmad-output`

两处内容有明确用途：

- `design-artifacts` 保存早期营销投放优化平台愿景；
- `_bmad-output` 保存已完成整改规格和延期事项。

它们不应删除，也不能覆盖当前源码与契约。旧规格中出现四段粒度、`units_sold`
或已删除的 `modules/mta` 属于历史语境，不是当前残留实现。

### 开发工具：`.agents` 与 `_bmad`

这是 BMad 6.8.0 及其扩展模块的安装结果：

- Core/BMM：6.8.0；
- TEA：v1.19.0；
- BMad Builder：v2.0.0；
- Automator：`main` 渠道；
- CIS：v0.2.1；
- GDS：v0.6.0；
- WDS：v0.4.3。

127 组相同内容主要是测试架构技能为独立分发而复制的知识文件，不是应删除的
普通重复文件。模板中的占位链接、占位 TOML 和 `TODO` 示例也属于生成器输入，
不能按业务文档规则直接清理。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| AMC MTA 单元与端到端测试 | 107/107 通过 |
| Ad Group 数量与预算模型测试 | 34/34 通过 |
| AMC 完整流水线 | 通过，五份输出可重建 |
| AMC/Ads 数据对齐 | 17/17 触点，90天、账户、国家、币种一致 |
| `_bmad` 配置解析测试 | 1/1 通过 |
| 安装技能目录与技能清单 | 119/119 对应 |
| Python 语法 | 105/105通过 |
| JavaScript 语法 | 7/7 通过 |
| Bash 入口语法 | 1/1 通过 |
| 项目自有 Markdown 本地链接 | 0 个断链 |
| JSON/TOML 实际配置 | 通过 |

工具测试补充：

- `test-scaffold-standalone-module.py` 为 8 通过、1 失败。实现输出插件名
  `exc`，测试却断言 `bmad-exc`，属于安装包内部契约不一致。
- `test_memlog.py` 需要 `pytest`，当前仓库没有依赖声明或本地环境，因此标记为
  未执行，不视为业务回归失败。

## 风险与优先级

1. **先补模型证据**：真实 AMC 可执行 SQL、滚动窗口、重采样和模型稳定性验证。
2. **再补工程基线**：依赖锁定、统一测试入口和 CI；当前仓库没有 CI/CD 或部署配置。
3. **工具问题隔离处理**：不要为修复上游技能测试而改动 AMC MTA。若确实使用
   standalone module scaffold，再单独修复插件命名契约并补齐 `pytest` 测试环境。
4. **保持分区边界**：当前事实以运行代码和模块契约为准；研究、历史愿景和旧规格
   继续只用于追溯。
