# 结果分析：SK-II Extended 27 维

| 项 | 内容 |
|----|------|
| 版本 | v1.5（主口径仅 27 维） |
| 指标 | `results/metrics.json` |
| 测试集 | 42 行，全部真实 July 11–31 |

## 1. 主结论

| 模型 | Test MAE | Test sMAPE | Test MAPE | Test R² |
|------|----------|------------|-----------|---------|
| MLP | 612 | 9.4% | 9.1% | **+0.46** |
| Multitask | 704 | 10.8% | 10.3% | **+0.27** |

验证集 Multitask：MAE 589，MAPE 8.6%，R² 0.69。

相对已废弃的 19 维：MAE/MAPE 约降 58%，R² 由负转正（历史对照见旧 `feature_compare_metrics.json` 若仍保留）。

## 2. 读数

- MAPE ~10%：粗预算情景可用，非精密日预报  
- 测集上本轮 MLP 略优于 Multitask；要漏斗诊断仍可用机制网  
- 训练多为合成：上线前纯真实重训  

## 3. 结构

输入 27；隐层 27→32→16；MLP 1441 / Multitask 1526 参数。见 `02`、`model_structure.json`。

## 4. 下一步

1. 多月真实 AMS，降低合成占比  
2. `campaigns` 预算上限 / 触顶特征  
3. Demo 已接 GMV Forecast；权重用 `export_demo_weights.py` 刷新后需重嵌 HTML（若要同步浏览器）
